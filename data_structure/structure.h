#include <stdlib.h>
#include <stdio.h>

typedef struct Key
{


} KEY;

typedef struct Node
{


} NODE;

typedef struct Tree
{


} TREE;

typedef struct Iterator
{


} ITER;

TREE * TreeCreate();
int TreeSize();
NODE  * AddNode();
NODE * GetNode();
NODE * TreeMin();
